
package slayt_kontrol;

import java.io.File;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import java.util.stream.Collectors;

public class Slayt_kontrol {

    public static void main(String[] args) throws IOException {
 

/*
        // Örnek liste tanımlaması
        List<Integer> liste1 = Arrays.asList(2, 50);
        
        // Listeyi stringe dönüştürme
        String s1 = liste1.toString();
        System.out.println("String temsili: " + s1); // Çıktı: [2, 50]

        // Listeyi diziye dönüştürme
        Integer[] dizi = liste1.toArray(new Integer[0]);
        System.out.println("Dizi elemanlari: " + Arrays.toString(dizi)); // Çıktı: [2, 50]


    List<Integer> sayiList = new ArrayList<>();
    sayiList.add(10);
    sayiList.add(11);
    sayiList.add(12);
    sayiList.add(13);
    sayiList.add(14);
    sayiList.add(15);
        
    List<Integer>
    filtresayiList = sayiList.stream()
    .filter(i -> i % 2 == 0).collect(Collectors.toList());System.out.println(filtresayiList);*/


   
        }
}


    

